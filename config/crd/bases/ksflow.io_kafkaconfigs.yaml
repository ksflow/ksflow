---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: kafkaconfigs.ksflow.io
spec:
  group: ksflow.io
  names:
    kind: KafkaConfig
    listKind: KafkaConfigList
    plural: kafkaconfigs
    shortNames:
    - kc
    singular: kafkaconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.topicPrefix
      name: Prefix
      type: string
    - jsonPath: .spec.configs.security\.protocol
      name: Protocol
      type: string
    - jsonPath: .status.phase
      name: Status
      type: string
    - jsonPath: .status.reason
      name: Reason
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KafkaConfig is the Schema for the kafkaconfigs API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: KafkaConfigSpec defines the desired state of KafkaConfig
            properties:
              configs:
                description: Kafka Configs for connecting to the cluster.
                properties:
                  bootstrap.servers:
                    description: The "bootstrap.servers" kafka config for connecting
                      to the cluster. For example "host1:port1,host2:port2".
                    pattern: ^([^,:]+):(\d+)(,([^,:]+):(\d+))*$
                    type: string
                  security.protocol:
                    description: The "security.protocol" kafka config for connecting
                      to the cluster, must be "PLAINTEXT" or "SSL".
                    enum:
                    - PLAINTEXT
                    - SSL
                    type: string
                required:
                - bootstrap.servers
                - security.protocol
                type: object
              topicPrefix:
                description: 'Prefix assigned to every kafka topic, resulting a topic
                  format matching: "<my-kafkaCluster-topicPrefix>.<my-namespace>.<my-kafkaTopic-name>"'
                maxLength: 125
                pattern: ^[a-z]([-a-z0-9\.]*[a-z])?$
                type: string
            required:
            - configs
            - topicPrefix
            type: object
          status:
            description: KafkaConfigStatus defines the observed state of KafkaConfig
            properties:
              lastUpdated:
                format: date-time
                type: string
              phase:
                description: KafkaConfigPhase defines the phase of the KafkaConfig
                enum:
                - ""
                - Available
                - Failed
                - Deleting
                type: string
              reason:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

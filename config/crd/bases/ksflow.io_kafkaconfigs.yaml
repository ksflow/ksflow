---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: kafkaconfigs.ksflow.io
spec:
  group: ksflow.io
  names:
    kind: KafkaConfig
    listKind: KafkaConfigList
    plural: kafkaconfigs
    shortNames:
    - kc
    singular: kafkaconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KafkaConfig is the Schema for the kafkaconfigs API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: KafkaConfigSpec defines the desired state of KafkaConfig
            properties:
              bootstrapServers:
                description: BootstrapServers is the Kafka "bootstrap.servers" for
                  connecting to the Kafka cluster (i.e. "host1:port1,host2:port2").
                pattern: ^([^,:]+):(\d+)(,([^,:]+):(\d+))*$
                type: string
            required:
            - bootstrapServers
            type: object
        type: object
    served: true
    storage: true
    subresources: {}

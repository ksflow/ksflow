---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: kafkatopics.ksflow.io
spec:
  group: ksflow.io
  names:
    kind: KafkaTopic
    listKind: KafkaTopicList
    plural: kafkatopics
    shortNames:
    - kt
    singular: kafkatopic
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.partitions
      name: Partitions
      type: string
    - jsonPath: .spec.replicationFactor
      name: Replicas
      type: string
    - jsonPath: .spec.reclaimPolicy
      name: ReclaimPolicy
      type: string
    - jsonPath: .status.fullTopicName
      name: TopicName
      priority: 10
      type: string
    - jsonPath: .status.phase
      name: Status
      type: string
    - jsonPath: .status.reason
      name: Reason
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KafkaTopic is the Schema for the kafkatopics API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: KafkaTopicSpec defines the desired state of KafkaTopic
            properties:
              configs:
                additionalProperties:
                  type: string
                description: 'Configs are the configs for the topic, see: https://kafka.apache.org/documentation/#topicconfigs
                  All values are specified as strings, since support for floating-point
                  varies across languages'
                type: object
              partitions:
                description: Partitions is the number of partitions in the topic.
                format: int32
                minimum: 1
                type: integer
              reclaimPolicy:
                default: Delete
                description: ReclaimPolicy defines what should happen to the underlying
                  kafka topic if the KafkaTopic is deleted.
                enum:
                - Delete
                - Retain
                type: string
              replicationFactor:
                description: ReplicationFactor is the number of replicas for each
                  of the topic's partitions.
                minimum: 1
                type: integer
            type: object
          status:
            description: KafkaTopicStatus defines the observed state of KafkaTopic
            properties:
              configs:
                additionalProperties:
                  type: string
                description: 'Configs are the configs for the topic, see: https://kafka.apache.org/documentation/#topicconfigs
                  All values are specified as strings, since support for floating-point
                  varies across languages'
                type: object
              fullTopicName:
                type: string
              kafkaConfigs:
                description: KafkaConfigs defines the Kafka configs for connecting
                  to the Kafka Cluster configs match what kafka clients use to connect.
                properties:
                  bootstrap.servers:
                    description: The "bootstrap.servers" kafka config for connecting
                      to the cluster. For example "host1:port1,host2:port2".
                    pattern: ^([^,:]+):(\d+)(,([^,:]+):(\d+))*$
                    type: string
                  security.protocol:
                    description: The "security.protocol" kafka config for connecting
                      to the cluster, must be "PLAINTEXT" or "SSL".
                    enum:
                    - PLAINTEXT
                    - SSL
                    type: string
                required:
                - bootstrap.servers
                - security.protocol
                type: object
              lastUpdated:
                format: date-time
                type: string
              partitions:
                description: Partitions is the number of partitions in the topic.
                format: int32
                minimum: 1
                type: integer
              phase:
                description: KafkaTopicPhase defines the phase of the KafkaTopic
                enum:
                - ""
                - Creating
                - Available
                - Failed
                - Deleting
                type: string
              reason:
                type: string
              replicationFactor:
                description: ReplicationFactor is the number of replicas for each
                  of the topic's partitions.
                minimum: 1
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: kafkatopics.ksflow.io
spec:
  group: ksflow.io
  names:
    kind: KafkaTopic
    listKind: KafkaTopicList
    plural: kafkatopics
    shortNames:
    - kt
    singular: kafkatopic
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.partitions
      name: Partitions
      type: string
    - jsonPath: .spec.replicationFactor
      name: Replicas
      type: string
    - jsonPath: .spec.reclaimPolicy
      name: ReclaimPolicy
      type: string
    - jsonPath: .status.phase
      name: Status
      type: string
    - jsonPath: .status.reason
      name: Reason
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KafkaTopic is the Schema for the kafkatopics API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: KafkaTopicSpec defines the desired state of KafkaTopic
            properties:
              configs:
                additionalProperties:
                  type: string
                description: 'Configs are the configs for the topic, see: https://kafka.apache.org/documentation/#topicconfigs
                  All values are specified as strings'
                type: object
              configsFrom:
                default:
                  configMapKeyRef:
                    key: topic.properties
                    name: ksflow-kafka-configs
                    optional: true
                description: ConfigsFrom is the source of the configs' value. Expects
                  a properties file containing the configs Values defined in Configs
                  with a duplicate key will take precedence.
                properties:
                  configMapKeyRef:
                    description: 'Selects the key of a ConfigMap containing the configs
                      for the topic in property file format, see: https://kafka.apache.org/documentation/#topicconfigs
                      Same behavior as in a container, see: https://github.com/kubernetes/website/blob/snapshot-initial-v1.26/content/en/examples/pods/pod-single-configmap-env-variable.yaml#L14'
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          TODO: Add other useful fields. apiVersion, kind, uid?'
                        type: string
                      optional:
                        description: Specify whether the ConfigMap or its key must
                          be defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              partitions:
                description: Partitions is the number of partitions in the topic.
                format: int32
                minimum: 1
                type: integer
              reclaimPolicy:
                default: Delete
                description: ReclaimPolicy defines what should happen to the underlying
                  kafka topic if the KafkaTopic is deleted.
                enum:
                - Delete
                - Retain
                type: string
              replicationFactor:
                description: ReplicationFactor is the number of replicas for each
                  of the topic's partitions.
                minimum: 1
                type: integer
            type: object
          status:
            description: KafkaTopicStatus defines the observed state of KafkaTopic
            properties:
              lastUpdated:
                format: date-time
                type: string
              phase:
                description: KafkaTopicPhase defines the phase of the KafkaTopic
                enum:
                - ""
                - Creating
                - Deleting
                - Available
                - Failed
                type: string
              reason:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

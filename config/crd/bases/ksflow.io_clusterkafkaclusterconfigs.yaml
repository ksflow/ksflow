---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: clusterkafkaclusterconfigs.ksflow.io
spec:
  group: ksflow.io
  names:
    kind: ClusterKafkaClusterConfig
    listKind: ClusterKafkaClusterConfigList
    plural: clusterkafkaclusterconfigs
    shortNames:
    - ckcc
    singular: clusterkafkaclusterconfig
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.topicPrefix
      name: Topic Prefix
      type: string
    - jsonPath: .spec.securityProtocol
      name: Security Protocol
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterKafkaClusterConfig is the Schema for the clusterkafkaclusterconfigs
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ClusterKafkaClusterConfigSpec defines the desired state of
              ClusterKafkaClusterConfig
            properties:
              topicPrefix:
                description: 'Prefix assigned to every kafka topic, resulting a topic
                  format matching: "<my-kafkaCluster-topicPrefix>.<my-namespace>.<my-kafkaTopic-name>"'
                maxLength: 125
                pattern: ^[a-z]([-a-z0-9\.]*[a-z])?$
                type: string
            required:
            - topicPrefix
            type: object
          status:
            description: ClusterKafkaClusterConfigStatus defines the observed state
              of ClusterKafkaClusterConfig
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
